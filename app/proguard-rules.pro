# Add project specific ProGuard rules here.
# This app ships with isMinifyEnabled = false by default, so this file is currently unused,
# but is kept so enabling minification later doesn't require creating it from scratch.
